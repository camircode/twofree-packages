{
  "name": "twofree-packages",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": "24.18.0",
    "pnpm": "11.13.1"
  },
  "scripts": {
    "build": "pnpm -r build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "pretypecheck": "pnpm --filter @camircode/twofree-database db:generate",
    "typecheck": "tsc --noEmit --project packages/core/tsconfig.json && tsc --noEmit --project packages/database/tsconfig.json && tsc --noEmit --project packages/auth/tsconfig.json && tsc --noEmit --project packages/data-provider/tsconfig.json && tsc --noEmit --project packages/application/tsconfig.json && tsc --noEmit --project packages/ui/tsconfig.json && tsc --noEmit --project tsconfig.base.json",
    "pretest": "pnpm --filter @camircode/twofree-database db:generate",
    "test": "pnpm -r test",
    "test:browser": "pnpm --filter @camircode/twofree-ui test:browser",
    "check": "pnpm format:check && pnpm lint && pnpm -r build && pnpm typecheck && pnpm -r test"
  },
  "devDependencies": {
    "@eslint/js": "9.39.1",
    "@types/node": "24.10.1",
    "eslint": "9.39.1",
    "prettier": "3.8.2",
    "tsx": "4.20.6",
    "typescript": "5.9.3",
    "typescript-eslint": "8.56.1",
    "vitest": "4.1.0"
  }
}
